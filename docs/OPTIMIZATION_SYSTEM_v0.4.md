# 优化系统 v0.4 - 完整文档

**创建时间**: 2026-01-16

## 🎯 系统概览

Gluttonous v0.4 引入了全自动多策略优化系统，支持：
- **6种训练策略**并行测试
- **多线程训练**和回测
- **自动参数优化**
- **8种仓位管理策略**
- **持续13小时优化**

---

## 📊 六种训练策略

### 1. Expanding Window（扩展窗口）
**已优化 ✅**

**特点**：
- 累积历史数据，训练集持续增长
- 使用样本权重，近期数据权重更高
- 适合长期趋势市场

**参数**：
```python
min_train_days: 60          # 最小训练天数
max_train_days: 500         # 最大训练天数
weight_decay_days: 20       # 权重衰减周期
weight_decay_rate: 0.95     # 权重衰减率
retrain_interval: 1         # 重训练间隔
```

---

### 2. Rolling Window（滚动窗口）
**新实现 🆕**

**特点**：
- 固定长度训练集（如60天）
- 窗口随时间向前滚动
- 适合市场环境快速变化的场景

**参数**：
```python
train_days: [40, 60, 90]     # 固定窗口大小
retrain_interval: [1, 3, 5]  # 重训练间隔
use_sample_weight: [True, False]  # 是否使用样本权重
```

**优势**：
- 快速适应市场变化
- 训练时间稳定
- 避免过度依赖历史数据

---

### 3. Adaptive Weight（自适应权重）
**新实现 🆕**

**特点**：
- 根据验证准确率动态调整权重衰减
- 准确率高时保留更多历史数据
- 准确率低时更关注近期数据

**参数**：
```python
min_train_days: 60
base_weight_decay: [0.95, 0.97]  # 基础衰减率
acc_threshold_high: [0.60, 0.65] # 高准确率阈值
acc_threshold_low: [0.50, 0.55]  # 低准确率阈值
retrain_interval: [1, 3]
```

**优势**：
- 智能自适应
- 根据模型表现动态调整
- 平衡历史规律和短期变化

---

### 4. Ensemble Multi-Scale（多尺度集成）
**新实现 🆕**

**特点**：
- 训练短期（20-30天）、中期（60-90天）、长期（120-180天）三个模型
- 加权集成预测结果
- 综合多个时间尺度的信息

**参数**：
```python
window_short: [20, 30]
window_medium: [60, 90]
window_long: [120, 180]
ensemble_weights: [[0.3, 0.4, 0.3], [0.4, 0.3, 0.3]]  # 集成权重
retrain_interval: [3, 5]
```

**优势**：
- 捕捉不同时间尺度的规律
- 降低单一模型的风险
- 提高预测稳定性

---

### 5. Volatility Adaptive（波动率自适应）
**新实现 🆕**

**特点**：
- 计算市场波动率指标
- 高波动期使用短窗口
- 低波动期使用长窗口

**参数**：
```python
min_train_days: 60
vol_lookback: [20, 30]           # 波动率回溯期
vol_high_threshold: [0.03, 0.04] # 高波动阈值
vol_low_threshold: [0.01, 0.015] # 低波动阈值
retrain_interval: [1, 3]
```

**优势**：
- 根据市场环境自动调整
- 高波动期快速反应
- 低波动期保持稳定

---

### 6. Momentum Enhanced（动量增强）
**新实现 🆕**

**特点**：
- LSTM预测基础概率
- 结合短期动量信号
- 加权组合两者结果

**参数**：
```python
min_train_days: 60
momentum_lookback: [5, 10, 20]   # 动量回溯期
momentum_weight: [0.2, 0.3, 0.4] # 动量权重
weight_decay_rate: [0.93, 0.95]
retrain_interval: [1, 3]
```

**优势**：
- 结合技术分析和机器学习
- 给予强势股票更高权重
- 提高选股质量

---

## 🔧 八种仓位管理策略

### 1. Equal Weight（等权重）
平均分配资金到所有股票

### 2. Probability Weighted（概率加权）
根据预测概率分配权重

### 3. Kelly Criterion（Kelly公式）
根据胜率和盈亏比计算最优仓位

### 4. Volatility Adjusted（波动率调整）
低波动股票高仓位，高波动股票低仓位

### 5. Risk Parity（风险平价）
均衡每只股票的风险贡献

### 6. Dynamic Sizing（动态仓位）
根据盈亏动态调整仓位大小

### 7. Concentration Limit（集中度限制）
限制单股票/行业占比

### 8. Adaptive Leverage（自适应杠杆）
根据市场状态调整杠杆

---

## 🚀 优化系统架构

### 核心组件

1. **MultiStrategyManager** - 管理6种训练策略配置
2. **ParallelExecutor** - 多线程并行训练和回测
3. **PositionManagerOptimizer** - 仓位管理策略优化
4. **ContinuousOptimizer** - 13小时持续优化调度器
5. **BacktestEngine** - 完整回测引擎

### 工作流程

```
┌─────────────────────────────────────────────────────────┐
│         Continuous Optimizer (13 hours)                 │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  Iteration Loop (每10分钟)                     │    │
│  │                                                 │    │
│  │  1. 选择策略 (6种中选择)                       │    │
│  │  2. 采样参数 (Exploitation 70% + Exploration 30%) │
│  │  3. 选择仓位策略 (8种中选择)                   │    │
│  │  4. 提交训练任务                               │    │
│  │                                                 │    │
│  │  ┌─────────────────────────────────────────┐  │    │
│  │  │  Parallel Executor (2 workers)          │  │    │
│  │  │                                          │  │    │
│  │  │  Worker 1:  训练 → 回测 → 评分          │  │    │
│  │  │  Worker 2:  训练 → 回测 → 评分          │  │    │
│  │  │                                          │  │    │
│  │  └─────────────────────────────────────────┘  │    │
│  │                                                 │    │
│  │  5. 收集结果                                   │    │
│  │  6. 更新最佳参数                               │    │
│  │  7. 定期保存进度 (每30分钟)                    │    │
│  │                                                 │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 评分机制

```python
score = (
    total_return * 0.4 +      # 总收益率权重 40%
    sharpe * 0.1 * 0.3 +      # 夏普比率权重 30%
    (1 - max_dd) * 0.2 +      # 回撤(反向)权重 20%
    win_rate * 0.1             # 胜率权重 10%
)
```

---

## 📁 文件结构

```
src/lstm/
├── optimization/                    # 优化系统
│   ├── continuous_optimization.py  # 持续优化主程序
│   ├── parallel_executor.py        # 并行执行器
│   ├── multi_strategy_config.py    # 多策略配置管理
│   └── position_manager_optimizer.py  # 仓位管理优化器
│
├── experiments/
│   ├── executors/                   # 策略执行器
│   │   ├── expanding_window.py     # 扩展窗口
│   │   ├── rolling_window.py       # 滚动窗口
│   │   ├── adaptive_weight.py      # 自适应权重
│   │   ├── ensemble_multi_scale.py # 多尺度集成
│   │   ├── volatility_adaptive.py  # 波动率自适应
│   │   └── momentum_enhanced.py    # 动量增强
│   │
│   └── metrics/
│       └── backtest_engine.py      # 回测引擎
│
└── data/
    └── results/
        ├── experiments/             # 实验结果
        ├── backtest/                # 回测结果
        └── optimization/            # 优化日志
            ├── optimization_log_*.json
            ├── best_params_*.json
            └── summary_report_*.md
```

---

## 🎮 使用方法

### 快速测试（5分钟）
```bash
python scripts/test_optimization_system.py
```

### 完整优化（13小时）
```bash
python scripts/start_13hour_optimization.py
```

### 查看结果
```bash
# 优化日志
cat src/lstm/data/results/optimization/optimization_log_*.json

# 最佳参数
cat src/lstm/data/results/optimization/best_params_expanding_window.json

# 总结报告
cat src/lstm/data/results/optimization/summary_report_*.md
```

---

## 📊 预期输出

### 优化日志示例
```json
{
  "start_time": "2026-01-16T21:00:00",
  "current_time": "2026-01-17T10:00:00",
  "iteration": 78,
  "total_tasks": 156,
  "completed_tasks": 142,
  "optimization_log": [
    {
      "iteration": 1,
      "timestamp": "2026-01-16T21:10:32",
      "strategy": "expanding_window",
      "score": 0.4523,
      "train_params": {...},
      "trading_params": {...},
      "backtest_metrics": {
        "total_return": 1.07,
        "sharpe_ratio": 1.82,
        "max_drawdown": 0.39,
        "win_rate": 0.59
      }
    },
    ...
  ]
}
```

### 最佳参数示例
```json
{
  "strategy": "expanding_window",
  "best_score": 0.4821,
  "best_train_params": {
    "min_train_days": 60,
    "max_train_days": 500,
    "weight_decay_days": 15,
    "weight_decay_rate": 0.93,
    "retrain_interval": 1
  },
  "best_trading_params": {
    "prob_threshold": 0.73,
    "trailing_stop_pct": 0.05,
    "max_holding_days": 10,
    "min_holding_days": 2
  },
  "optimization_history": [...]
}
```

---

## ⚙️ 配置调整

### 调整运行时长
```python
# scripts/start_13hour_optimization.py
DURATION_HOURS = 13.0  # 修改为所需时长
```

### 调整并行数
```python
MAX_WORKERS = 2  # 建议2-3个，避免GPU冲突
```

### 调整保存间隔
```python
SAVE_INTERVAL = 30  # 每30分钟保存一次
```

---

## 🎯 优化技巧

### 1. 参数采样策略
- **Exploitation（70%）**：基于当前最佳参数微调
  - 随机选择1-2个参数
  - 选择相邻值或小幅调整
- **Exploration（30%）**：随机探索参数空间
  - 完全随机采样所有参数
  - 发现新的优秀配置

### 2. 仓位管理优化
- 动态测试8种仓位策略
- 70%概率使用当前最佳策略
- 30%概率探索其他策略

### 3. 自动调整
- 剩余时间 < 2小时：只测试微调
- 剩余时间 2-6小时：中等探索
- 剩余时间 > 6小时：多样化探索

---

## 🔍 监控和调试

### 实时监控
```bash
# 查看优化进度
tail -f src/lstm/data/results/optimization/optimization_log_*.json

# 监控GPU使用
nvidia-smi -l 1
```

### 中断和恢复
- 按 `Ctrl+C` 安全停止
- 所有进度自动保存
- 可从最佳参数继续优化

---

## 📈 性能预期

### 单个任务耗时
- 训练：30-60分钟（取决于数据量）
- 回测：5-10分钟
- 总计：35-70分钟/任务

### 13小时预期
- 2个workers并行
- 每小时完成约 2-3 个任务
- 13小时总计：**26-39 个配置测试**

### 策略分布
- 6种策略 × (26-39)/6 ≈ **每种策略测试4-6次**
- 足够探索参数空间
- 找到每种策略的最优配置

---

## ✅ 验证清单

运行前确认：
- [ ] GPU可用 (`nvidia-smi`)
- [ ] 数据已清洗 (`.pipeline_data/features/`)
- [ ] 磁盘空间充足 (>10GB)
- [ ] 网络稳定（可选，用于远程监控）

运行中监控：
- [ ] 每30分钟查看保存进度
- [ ] GPU温度正常 (<80°C)
- [ ] 无内存溢出

运行后分析：
- [ ] 查看总结报告
- [ ] 对比各策略最佳配置
- [ ] 选择最优策略进行生产部署

---

## 🎓 进阶功能

### 自定义策略
1. 创建新的执行器类（继承`BaseStrategyExecutor`）
2. 实现`prepare_data`和`should_retrain`方法
3. 在`multi_strategy_config.py`中添加配置
4. 在`parallel_executor.py`中注册执行器

### 自定义评分函数
修改`parallel_executor.py`中的`_calculate_score`方法：
```python
def _calculate_score(self, backtest_result: Dict) -> float:
    # 自定义权重
    return (
        total_return * 0.5 +     # 更重视收益
        sharpe * 0.2 +           # 降低夏普权重
        (1 - max_dd) * 0.2 +
        win_rate * 0.1
    )
```

---

## 📞 故障排除

### Q: GPU内存不足
A: 降低`batch_size`或`max_workers`

### Q: 训练过慢
A: 减少`max_train_days`或增加`retrain_interval`

### Q: 磁盘空间不足
A: 清理旧的实验结果：
```bash
rm -rf src/lstm/data/results/experiments/*_20260115*
```

### Q: 进程卡死
A: 检查GPU状态，重启CUDA：
```bash
nvidia-smi --gpu-reset
```

---

**文档版本**: v0.4.0
**最后更新**: 2026-01-16
**作者**: Claude Code + Gluttonous Team
